# URL del backend Node.js (Express + MongoDB)
backend_url=http://localhost:3030

# URL del microservicio de análisis de sentimientos (Code Engine o simulación)
# Si no tienes Code Engine, usaremos una simulación local (explicado después)
sentiment_analyzer_url=http://localhost:5000/ # Simulación 